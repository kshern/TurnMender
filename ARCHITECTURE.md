# TurnMender 架构说明

TurnMender 是一个专为 Codex 桌面端设计的 Tauri 2 应用。前端负责展示和操作，Rust 后端负责读取 Codex 本地任务记录、判断是否需要继续、维护状态，并在 macOS 上通过本地消息通道发起下一轮。

## 设计原则

- 所有判断都在本机完成，不上传任务内容。
- 只有明确的容量错误，并且该轮明确没有最终回复时，才允许自动继续。
- 所有发送都使用确定的任务 ID，不根据窗口、焦点或标题猜测目标。
- 消息通道不可用或回执不完整时停止自动处理，不切换到命令行或界面自动化。
- 关键状态写入本地文件，应用重启后不会重复处理同一条错误。

## 代码分层

```text
src/
├── main.ts                 界面渲染、定时刷新和用户操作
├── i18n.ts                 中英文文案
└── styles.css              界面样式

src-tauri/src/
├── core/
│   ├── classifier.rs       容量错误与最终回复判断
│   ├── policy.rs           去重和连续自动继续保护
│   └── state.rs            任务状态模型
├── watcher/mod.rs          增量读取和解析 JSONL 任务记录
├── transport/
│   └── mac_desktop_ipc.rs  macOS 本地消息通道
├── service.rs              后台轮询和整体处理流程
├── storage.rs              设置、状态和日志持久化
└── lib.rs                  Tauri 命令、窗口和托盘生命周期
```

## 处理流程

```text
Codex JSONL 任务记录
        │
        ▼
SessionWatcher 增量读取完整行
        │
        ▼
classifier 判断错误类型和 last_agent_message
        │
        ├── 普通错误或已有最终回复 ──► 不发送
        ├── 关键字段缺失 ───────────► 提示人工确认
        └── 容量错误且最终回复为空
                    │
                    ▼
            policy 去重与次数保护
                    │
                    ▼
          macOS 本地消息通道发送
                    │
        ┌───────────┴───────────┐
        ▼                       ▼
  返回明确的新轮次 ID      失败或回执不完整
        │                       │
        ▼                       ▼
  记录成功和连续次数       停止补发并提示人工处理
```

## 任务记录监听

任务目录优先取自 `CODEX_HOME`，未设置时使用 `~/.codex/sessions`。

监听器按文件保存读取位置，只处理完整 JSONL 行，并处理文件追加、截断和半行。启动时只检查最近 10 分钟内更新过的记录，避免把很久以前的失败当作新事件。

当前识别的关键记录包括：

- 用户开始新一轮；
- 任务开始；
- 带有完成时间、错误和 `last_agent_message` 的任务结束；
- 任务名称和项目路径信息。

## 自动继续判断

容量错误满足以下任一条件时才会进入进一步判断：

- 错误信息是 `server_overloaded`；
- 错误文字包含已知的模型容量提示。

随后根据 `last_agent_message` 处理：

- 明确为空或 `null`：允许进入待继续流程；
- 有内容：认为该轮已有最终回复；
- 字段缺失或格式未知：转为人工确认。

## 去重与循环保护

- 使用“任务 ID + 失败轮次 ID”标识已处理事件。
- 只有收到包含新轮次 ID 的成功回执，才记录为自动继续成功。
- 同一任务连续自动继续达到配置上限后转为人工处理；可配置范围为 1–100 次，默认 10 次。
- 用户手动开始新一轮时，清空该任务的连续计数并取消旧候选事件。
- 已处理事件和自动轮次会定期裁剪，避免状态文件无限增长。

## 平台通道

### macOS

后端连接 `$CODEX_HOME/ipc/ipc.sock`，未设置 `CODEX_HOME` 时使用 `~/.codex/ipc/ipc.sock`。连接前会确认目标是当前用户拥有的 Unix socket，并检查父目录不存在组或其他用户可写权限。

发送流程先按任务 ID 查找持有该任务的桌面客户端，再请求启动新轮次。只有协议返回成功并携带新的轮次 ID，才认为发送完成。

### Windows

当前共用任务监听、判断、状态和界面逻辑。由于尚未确认可靠的定向消息通道，Windows 不会自动发送，只提示用户人工继续。

## 本地数据

本地数据统一保存在平台的 TurnMender 数据目录：

| 文件 | 内容 |
| --- | --- |
| `state.json` | 已处理事件、自动轮次和连续计数 |
| `config.json` | 自动继续开关、连续自动继续次数上限和自定义继续指令 |
| `turnmender.log` | 关键处理结果和异常 |

macOS 目录是 `~/Library/Application Support/TurnMender`，Windows 目录是 `%LOCALAPPDATA%\TurnMender`。日志超过 5 MB 时轮换，并保留一个 `.1` 备份。

状态和日志不保存任务正文。日志只记录任务 ID、轮次 ID、处理结果和必要的错误信息。

## 前端与托盘

前端通过 Tauri 命令读取后端快照、切换自动继续、保存设置、移除最近任务、打开任务和查看日志。顶部保留自动继续快速开关，其他低频选项集中在设置弹窗中。关闭主窗口只会隐藏窗口，后台服务和托盘继续运行。

托盘菜单根据同一份后端快照动态生成：顶部显示具体状态和自动继续勾选开关；最多列出 3 个待处理任务并支持按任务 ID 直接打开原 Codex 任务；下方提供主窗口、设置、日志和退出入口。任务或设置变化时只在菜单内容实际改变后重建菜单。

托盘颜色由运行状态、消息通道、自动继续开关和待处理任务共同决定：

- 绿色：监听和通道正常；
- 黄色：暂停、通道未就绪或存在待处理任务；
- 红色：监听失败或需要人工确认。

## 验证

前端使用 TypeScript 检查和 Vite 构建：

```sh
pnpm build
```

Rust 后端的单元测试覆盖错误分类、任务状态、去重策略、增量读取、日志轮换和消息通道安全检查：

```sh
cd src-tauri
cargo test --all-targets
cargo check --all-targets
```
